1.create a folder
mkdir projectname
it created an empty project to the current direct.


2.cd=>change directory
cd 

3.git init=>creates an empty repo

4. 
commit envrionment
staging/stash environment

git add index.html
git add .
git add arca.txt

5.git commit -m "first commit "
git commit -m "added boilerplate code"
git commit -m "added header component"
git commit -m "made changes to header component"
git commit -m "made 2 function inside header component"

git log

How to create a pull request in git?4
1.Fork lavish repo
1.git clone  https://github.com/username/vscodegitdemo
One you clone the repo, 
1.creating a new branch
git checkout -b new_branch
2.create a new remote for the upstream repo
upstream=>original repo
git remote add upstream  https://github.com/username/vscodegit

code=>Create a pull request
i will merge a request created by you if it is working perfect

git push -u origin main